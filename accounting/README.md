# محاسبة OLV — نظام محاسبي سحابي للمطعم

نظام محاسبة بسيط شغّال بالكامل من المتصفح (بدون سيرفر خاص بيك)، متصل بقاعدة
بيانات سحابية حقيقية على [Supabase](https://supabase.com) (مجاني للمشاريع
الصغيرة)، ومنشور تلقائيًا عن طريق GitHub Pages بتاعت الريبو ده.

## الميزات

- تسجيل دخول آمن للموظفين (Supabase Auth)
- **بيع سريع**: شاشة تعرض أصناف المنيو بالصور والأسعار والإضافات، تُبنى منها
  سلة وتُحوَّل تلقائيًا لقيد مبيعات
- **المبيعات**: نفس فكرة البيع السريع لكن بشكل قائمة/نموذج (مناسبة لإدخال
  عدة عمليات بيع بسرعة أو لمراجعة السجل)
- **المخزون**: مواد خام (لحمة، جبنة، خبز...) بكمياتها ووحداتها، ووصفة لكل
  صنف/إضافة تحدد قد إيه بتستهلك من كل مادة — البيع من أي شاشة بيع بينزّل
  المخزون تلقائيًا حسب الوصفة
- **المصروفات**: تسجيل المصروفات وتصنيفها (مشتريات، رواتب، إيجار...)
- **الخزينة**: تقفيل يومي للكاش ومقارنة المتوقع بالفعلي
- **التقارير**: أرباح وخسائر لأي فترة، توزيع المصروفات حسب التصنيف، وتصدير
  التقرير كـ PDF (زرار "تصدير PDF" بيفتح نافذة الطباعة — اختر "حفظ كـ PDF")
- كل البيانات محفوظة على السحابة (Supabase) ومتاحة من أي جهاز/متصفح

## خطوات التشغيل (مرة واحدة)

1. **أنشئ مشروع Supabase مجاني**
   - روح على https://supabase.com وسجّل واعمل New Project.
   - سيب المشروع يخلص التجهيز (دقيقة أو اتنين).

2. **جهّز قاعدة البيانات**
   - من لوحة تحكم Supabase: **SQL Editor → New query**.
   - شغّل الملفات دي **بالترتيب** (كل ملف مرة واحدة، الصق المحتوى واضغط Run):
     1. [`schema.sql`](./schema.sql) — الجداول الأساسية (مبيعات، مصروفات، خزينة)
     2. [`schema-products-addons.sql`](./schema-products-addons.sql) — المنتجات والإضافات (لازمة لشاشة البيع السريع)
     3. [`schema-inventory.sql`](./schema-inventory.sql) — المخزون ووصفات الأصناف
   - لو موبايلك بيحوّل `--` (شرطتين) لشرطة واحدة عند اللصق وطلعت رسالة
     syntax error، استخدم [`schema-no-comments.sql`](./schema-no-comments.sql)
     بدل `schema.sql` (نفس المحتوى بدون تعليقات).

3. **اربط الموقع بالمشروع**
   - من Supabase: **Project Settings → API**.
   - انسخ **Project URL** و **anon public key**.
   - افتح ملف [`config.js`](./config.js) وحط القيمتين مكان:
     - `YOUR_SUPABASE_PROJECT_URL`
     - `YOUR_SUPABASE_ANON_PUBLIC_KEY`
   - احفظ وارفع (commit + push) الملف على GitHub — الموقع هيتحدث تلقائيًا
     عن طريق الـ workflow الموجود بالفعل (`.github/workflows/static.yml`).

4. **اعمل حساب لصاحب المطعم (Owner) وللموظفين**
   - من Supabase: **Authentication → Users → Add user**.
   - حط الإيميل وكلمة سر لكل موظف/كاشير محتاج يدخل النظام.
   - أول ما أي حساب يتعمل، بيتحط تلقائيًا في جدول `profiles` بدور `staff`.
   - لو عايز تدي حد صلاحية "owner" أو "manager": من **Table Editor → profiles**
     عدّل عمود `role` للمستخدم يدويًا.

5. **افتح النظام وجهّز المنيو والمخزون**
   - `https://<اسم-المستخدم>.github.io/<اسم-الريبو>/accounting/login.html`
   - من **الإعدادات**: أضف أصناف المنيو (اسم، سعر، تصنيف، صورة) والإضافات
   - من **المخزون → المواد الخام**: أضف كل خامة (لحمة، خبز، جبنة...) بوحدتها وكميتها
   - من **المخزون → وصفات الأصناف**: لكل صنف/إضافة حدد الخامات وكمية كل
     واحدة تُستهلك عند بيع وحدة واحدة — من غير الخطوة دي البيع مش هينزّل
     أي حاجة من المخزون (البيع هيسجّل عادي بس من غير خصم خامات)

## ملاحظات أمان

- مفتاح `anon key` مصمم يكون ظاهر في المتصفح — الحماية الحقيقية جاية من
  سياسات RLS في `schema.sql` اللي بتشترط تسجيل الدخول لأي عملية قراءة/كتابة.
- لو عايز تفصل بين صلاحيات owner/manager/staff بشكل أدق (مثلاً بس المدير
  يقدر يمسح قيود)، عدّل سياسات RLS في `schema.sql` على أساس عمود `role`.
- الريبو ده لازم يفضل **private** لو مش عايز أي حد يشوف كود الموقع، رغم إن
  بيانات المحاسبة نفسها محمية بتسجيل الدخول بغض النظر عن ده.

## هيكل الملفات

```
accounting/
  login.html                     صفحة تسجيل الدخول
  index.html                     لوحة التحكم الرئيسية (ملخص اليوم)
  pos.html                       بيع سريع (شبكة أصناف بالصور)
  sales.html                     بيع بشكل قائمة/نموذج + سجل المبيعات
  inventory.html                 المواد الخام ووصفات الأصناف/الإضافات
  expenses.html                  تسجيل وعرض المصروفات
  cash-register.html             تقفيل الخزينة اليومي
  reports.html                   تقارير الأرباح والخسائر + تصدير PDF
  settings.html                  تصنيفات المصروفات، المنتجات، الإضافات
  schema.sql                     الجداول الأساسية
  schema-products-addons.sql     المنتجات والإضافات
  schema-inventory.sql           المخزون والوصفات + دالة record_sale
  config.js                      بيانات الاتصال بمشروع Supabase بتاعك
  assets/                        التصميم (theme.css) والأكواد المشتركة (JS)
```

## ملاحظة عن حذف عمليات بيع

حذف قيد من سجل المبيعات (sales.html) بيمسح القيد بس مش بيرجّع الخامات
المخصومة للمخزون تلقائيًا — لو حصل حذف بالغلط، عدّل كمية المخزون يدويًا
من صفحة المخزون.

## تطوير مستقبلي (اختياري)

- تنبيه تلقائي (إشعار) لما مادة خام توصل لحد التنبيه
- ربط الأدوار (owner/manager/staff) بصلاحيات أدق داخل الواجهة نفسها
- دعم أكثر من فرع للمطعم
